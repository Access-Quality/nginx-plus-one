# nginx-plus-one-eks

Terraform + GitHub Actions to provision an AWS EKS cluster using Terraform Cloud (local execution).

- Terraform config: `terraform/eks`
- GitHub Actions workflow: `.github/workflows/eks-cluster.yml`

This project intentionally does not deploy NGINX.
